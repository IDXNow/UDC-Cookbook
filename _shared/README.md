# Shared Assets

Reusable configurations and utilities used across multiple recipes.

## Contents

- **sample_configs/** — Example `default_config.json` variants (cloud, local, etc.)
- **utilities/** — Helper scripts if needed
